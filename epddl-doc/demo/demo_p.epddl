(define (problem demo)
(:domain demo)
(:objects
	aa
	bb
	cc - pos
)
(:init (and
  (K (and (imply (doorOpen) (inRoom)) 
          (imply (not (lightOn)) (not(inRoom)))
      )
  )
  (DK (inRoom))
  (DK (not (inRoom)))
  (DK (lightOn))
  (DK (not (lightOn)))
  )
)

(:goal (oneof (a) (b) (c))
  
)
)
